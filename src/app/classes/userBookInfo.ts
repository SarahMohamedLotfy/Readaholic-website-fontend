/** receive the data of user ralated book info from the json file */
export class userBookInfo {
    /** user rating of a book */
    rating: number;
    /**the shelf where the user stores aa book */
    shelf_name:number;
    /**the body of the review made by the user on a book */
    body: string;
    /**review id */
    id: number;
}